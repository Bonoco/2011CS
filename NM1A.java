import java.util.Scanner;
public class NameGenerator1{
  public static void main(String[]args){
    Scanner in = new Scanner(System.in);

     int i;
     int letter = (int)(65 + Math.random() * (90 - 65 + 1));
     int number = (int)(0 + Math.random() * 100)

    System.out.print("Enter the length of the name you want: ");

    int limit = in.nextInt();

    if(limit < 1){
      System.out.println("Must be at least one letter and must be a number for the length ");
    }else if (number > 20){
      System.out.println(""+"" (char)letter " "+ "" (char)letterTwo " " + "has a nice ring to it");
    }else if (number<20){
      System.out.println(hmmm, )
    for (i=1 ; i <= limit; i++){
      int letterOne = (int)(65 + Math.random() * (90 - 65 + 1));
      int letterTwo = (int)(97 + Math.random() * (90 - 65 + 1));

      String word = (""+"" (char)letter " "+ "" (char)letterTwo " " + "has a nice ring to it");


      System.out.println((char)letterTwo + "");
      System.out.print("");



     }
    }

  }
}
